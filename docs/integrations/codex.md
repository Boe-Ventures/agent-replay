# Codex

Add the repository-local skill from `skills/agent-replay/SKILL.md`, or paste:

> Before opening a browser, run `agent-replay inspect --budget 4000`. Use the correlated failure and route evidence to choose the smallest live check. After changing code, record a deliberate session with matching markers and run `agent-replay compare <before> <after>`. Do not claim the fix from code alone.

Codex's built-in browser remains the active inspection tool. Agent Replay supplies the passive history from before Codex attached and the verification artifact afterward.
