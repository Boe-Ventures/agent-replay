# Continue

Add a project rule:

> When a UI task mentions a failure, run `agent-replay inspect --budget 2500`. Quote event IDs and offsets when proposing a cause. After a fix, compare the marked before/after sessions.

For tool calling, run `agent-replay mcp` as a local stdio MCP server. The five-tool surface is intentionally small.
