# OpenHands

Expose `agent-replay` on the shell path and include:

> Read `.agent-replay/latest/summary.md` or run `agent-replay inspect --budget 3000` before interacting with the browser. Prefer the JSONL signal files over screenshots when they answer the question. Pack the final verification with `agent-replay pack`.

No custom OpenHands plugin is required.
